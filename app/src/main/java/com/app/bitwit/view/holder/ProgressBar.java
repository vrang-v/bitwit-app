package com.app.bitwit.view.holder;

import androidx.recyclerview.widget.RecyclerView;
import com.app.bitwit.databinding.ProgressBarBinding;

public class ProgressBar extends RecyclerView.ViewHolder {
    
    public ProgressBar(ProgressBarBinding binding) {
        super(binding.getRoot( ));
    }
    
}
